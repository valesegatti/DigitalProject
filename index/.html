import { motion } from "framer-motion";
import { ChevronDown, Send, CheckCircle2, Users, Target, Zap } from "lucide-react";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { ThemeToggle } from "@/components/ThemeToggle";
import { ContactForm } from "@/components/ContactForm";
import { CaseStudies } from "@/components/CaseStudies";

/**
 * Design Philosophy: Modern, Professional, Human-Centric
 * - Typography: Playfair Display for headings (bold, elegant), Sora for body (clean, readable)
 * - Colors: Deep blue (#0F172A) as primary, emerald green (#10B981) as accent
 * - Motion: Subtle, purposeful animations that guide attention
 * - Layout: Asymmetric, flowing sections with generous whitespace
 */

const containerVariants = {
  hidden: { opacity: 0 },
  visible: {
    opacity: 1,
    transition: {
      staggerChildren: 0.2,
      delayChildren: 0.3,
    },
  },
};

const itemVariants = {
  hidden: { opacity: 0, y: 20 },
  visible: {
    opacity: 1,
    y: 0,
    transition: { duration: 0.8 },
  },
};

export default function Home() {
  const [email, setEmail] = useState("");
  const [feedbackSubmitted, setFeedbackSubmitted] = useState(false);

  const handleFeedbackSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    if (email.trim()) {
      setFeedbackSubmitted(true);
      setEmail("");
      setTimeout(() => setFeedbackSubmitted(false), 3000);
    }
  };

  return (
    <div className="min-h-screen bg-background text-foreground">
      {/* Navigation */}
      <nav className="sticky top-0 z-50 bg-background/80 backdrop-blur-md border-b border-border">
        <div className="container flex items-center justify-between h-16">
          <motion.div
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            className="text-2xl font-bold"
            style={{ fontFamily: "Playfair Display" }}
          >
            Vale Segatti
          </motion.div>
          <div className="flex items-center gap-8">
            <div className="hidden md:flex gap-8">
              {["Approccio", "Servizi", "Contatti"].map((item) => (
                <motion.a
                  key={item}
                  href={`#${item.toLowerCase()}`}
                  className="text-sm font-medium hover:text-accent transition-smooth"
                  whileHover={{ scale: 1.05 }}
                >
                  {item}
                </motion.a>
              ))}
            </div>
            <ThemeToggle />
          </div>
        </div>
      </nav>

      {/* Hero Section */}
      <section className="relative min-h-screen flex items-center overflow-hidden">
        <div
          className="absolute inset-0 z-0"
          style={{
            backgroundImage:
              "url('https://d2xsxph8kpxj0f.cloudfront.net/310519663629168585/2PoPXpkUQD2fwnKo7Cydp8/hero-security-network-VFfufygDQT2fSCpRxZqHfg.webp')",
            backgroundSize: "cover",
            backgroundPosition: "center",
            opacity: 0.15,
          }}
        />
        <div className="absolute inset-0 bg-gradient-to-br from-background via-background/80 to-background/60 z-1" />

        <motion.div
          className="container relative z-10 py-20"
          variants={containerVariants}
          initial="hidden"
          animate="visible"
        >
          <motion.div variants={itemVariants} className="max-w-3xl">
            <span className="inline-block text-sm font-semibold text-accent mb-4">
              HUMAN-CENTRIC SECURITY ENGINEERING
            </span>
            <h1
              className="display-lg mb-6 leading-tight"
              style={{ fontFamily: "Playfair Display" }}
            >
              Proteggere i Dati attraverso l'Esperienza Umana
            </h1>
            <p className="body-lg text-muted-foreground mb-8 max-w-2xl">
              La sicurezza non è un prodotto da installare. È una consapevolezza da costruire, una cultura da coltivare, un'esperienza da vivere. Trasformiamo la complessità tecnica in azioni umane concrete.
            </p>
          </motion.div>

          <motion.div variants={itemVariants} className="flex gap-4 mb-16">
            <Button
              size="lg"
              className="bg-accent hover:bg-accent/90 text-white"
            >
              Scopri il Mio Approccio
            </Button>
            <Button
              size="lg"
              variant="outline"
              className="border-accent text-accent hover:bg-accent/10"
            >
              Contattami
            </Button>
          </motion.div>

          <motion.div variants={itemVariants} className="flex items-center gap-2 text-muted-foreground">
            <span>Scorri per scoprire di più</span>
            <motion.div animate={{ y: [0, 8, 0] }} transition={{ duration: 2, repeat: Infinity as any }}>
              <ChevronDown className="w-5 h-5" />
            </motion.div>
          </motion.div>
        </motion.div>
      </section>

      {/* User Experience Section */}
      <section className="py-24 bg-card/30">
        <motion.div
          className="container"
          variants={containerVariants}
          initial="hidden"
          whileInView="visible"
          viewport={{ once: true }}
        >
          <motion.div variants={itemVariants} className="mb-16">
            <span className="text-accent font-semibold text-sm">USER EXPERIENCE</span>
            <h2
              className="heading-xl mt-2 mb-4"
              style={{ fontFamily: "Playfair Display" }}
            >
              Un'Esperienza Indimenticabile
            </h2>
            <p className="body-lg text-muted-foreground max-w-2xl">
              La sicurezza non deve essere complicata. Trasformiamo processi complessi in esperienze intuitive e coinvolgenti che i tuoi team amano usare.
            </p>
          </motion.div>

          <div className="grid md:grid-cols-3 gap-8">
            {[
              {
                icon: Zap,
                title: "Intuitiva",
                description: "Interfacce e processi che si capiscono al primo sguardo",
              },
              {
                icon: Users,
                title: "Collaborativa",
                description: "Strumenti che uniscono i team attorno alla sicurezza",
              },
              {
                icon: Target,
                title: "Efficace",
                description: "Risultati misurabili che proteggono veramente",
              },
            ].map((item, idx) => (
              <motion.div
                key={idx}
                variants={itemVariants}
                whileHover={{ y: -8 }}
              >
                <Card className="p-8 h-full border-border/50 hover:border-accent/50 transition-smooth">
                  <item.icon className="w-12 h-12 text-accent mb-4" />
                  <h3 className="heading-md mb-3">{item.title}</h3>
                  <p className="text-muted-foreground">{item.description}</p>
                </Card>
              </motion.div>
            ))}
          </div>
        </motion.div>
      </section>

      {/* User Persona Section */}
      <section className="py-24">
        <motion.div
          className="container"
          variants={containerVariants}
          initial="hidden"
          whileInView="visible"
          viewport={{ once: true }}
        >
          <motion.div variants={itemVariants} className="mb-16">
            <span className="text-accent font-semibold text-sm">USER PERSONA</span>
            <h2
              className="heading-xl mt-2 mb-4"
              style={{ fontFamily: "Playfair Display" }}
            >
              Conosco il Tuo Team
            </h2>
            <p className="body-lg text-muted-foreground max-w-2xl">
              Ogni persona nella tua organizzazione ha esigenze diverse. Creo soluzioni personalizzate per manager, ingegneri, dipendenti e analisti di sicurezza.
            </p>
          </motion.div>

          <motion.div
            className="rounded-2xl overflow-hidden"
            variants={itemVariants}
            whileInView={{ opacity: 1, y: 0 }}
            initial={{ opacity: 0, y: 20 }}
            viewport={{ once: true }}
          >
            <img
              src="https://d2xsxph8kpxj0f.cloudfront.net/310519663629168585/2PoPXpkUQD2fwnKo7Cydp8/user-persona-illustration-RL5uKmLcL2yxiueW8KMjCe.webp"
              alt="User Personas"
              className="w-full h-auto"
            />
          </motion.div>
        </motion.div>
      </section>

      {/* Customer Journey Section */}
      <section className="py-24 bg-card/30">
        <motion.div
          className="container"
          variants={containerVariants}
          initial="hidden"
          whileInView="visible"
          viewport={{ once: true }}
        >
          <motion.div variants={itemVariants} className="mb-16">
            <span className="text-accent font-semibold text-sm">CUSTOMER JOURNEY</span>
            <h2
              className="heading-xl mt-2 mb-4"
              style={{ fontFamily: "Playfair Display" }}
            >
              Il Tuo Percorso verso la Resilienza
            </h2>
            <p className="body-lg text-muted-foreground max-w-2xl">
              Non è un progetto singolo, è un viaggio. Ti accompagno attraverso ogni fase: dalla consapevolezza iniziale all'implementazione, fino al miglioramento continuo.
            </p>
          </motion.div>

          <motion.div
            className="rounded-2xl overflow-hidden"
            variants={itemVariants}
            whileInView={{ opacity: 1, y: 0 }}
            initial={{ opacity: 0, y: 20 }}
            viewport={{ once: true }}
          >
            <img
              src="https://d2xsxph8kpxj0f.cloudfront.net/310519663629168585/2PoPXpkUQD2fwnKo7Cydp8/customer-journey-map-9wdvP9cBogDvyxkoa8qUcx.webp"
              alt="Customer Journey"
              className="w-full h-auto"
            />
          </motion.div>
        </motion.div>
      </section>

      {/* Value Proposition Section */}
      <section className="py-24">
        <motion.div
          className="container"
          variants={containerVariants}
          initial="hidden"
          whileInView="visible"
          viewport={{ once: true }}
        >
          <motion.div variants={itemVariants} className="mb-16 max-w-3xl">
            <span className="text-accent font-semibold text-sm">VALUE PROPOSITION</span>
            <h2
              className="heading-xl mt-2 mb-4"
              style={{ fontFamily: "Playfair Display" }}
            >
              Cosa Mi Rende Diverso
            </h2>
          </motion.div>

          <div className="grid md:grid-cols-2 gap-12">
            <motion.div variants={itemVariants}>
              <Card className="p-8 border-border/50 h-full">
                <h3 className="heading-md mb-4 flex items-center gap-2">
                  <CheckCircle2 className="w-6 h-6 text-accent" />
                  Approccio Olistico
                </h3>
                <p className="text-muted-foreground mb-4">
                  Non separo la tecnologia dalle persone. Combino ingegneria elettronica, coaching sulla sicurezza e comunicazione tecnica in un'unica soluzione coerente.
                </p>
                <ul className="space-y-2 text-sm text-muted-foreground">
                  <li>✓ Audit di processi e persone</li>
                  <li>✓ Coaching culturale sulla sicurezza</li>
                  <li>✓ Documentazione tecnica comprensibile</li>
                </ul>
              </Card>
            </motion.div>

            <motion.div variants={itemVariants}>
              <Card className="p-8 border-border/50 h-full">
                <h3 className="heading-md mb-4 flex items-center gap-2">
                  <CheckCircle2 className="w-6 h-6 text-accent" />
                  Risultati Misurabili
                </h3>
                <p className="text-muted-foreground mb-4">
                  Ogni intervento è tracciato, misurato e ottimizzato. Trasformo la consapevolezza in metriche concrete che dimostrano il valore della sicurezza.
                </p>
                <ul className="space-y-2 text-sm text-muted-foreground">
                  <li>✓ Resilience Score migliorato</li>
                  <li>✓ Riduzione dei rischi quantificata</li>
                  <li>✓ ROI della sicurezza documentato</li>
                </ul>
              </Card>
            </motion.div>
          </div>
        </motion.div>
      </section>

      {/* Feedback Section */}
      <section id="feedback" className="py-24 bg-gradient-to-br from-accent/10 to-accent/5">
        <motion.div
          className="container max-w-2xl"
          variants={containerVariants}
          initial="hidden"
          whileInView="visible"
          viewport={{ once: true }}
        >
          <motion.div variants={itemVariants} className="text-center mb-12">
            <span className="text-accent font-semibold text-sm">FEEDBACK</span>
            <h2
              className="heading-xl mt-2 mb-4"
              style={{ fontFamily: "Playfair Display" }}
            >
              Rimani Aggiornato
            </h2>
            <p className="body-lg text-muted-foreground">
              La sicurezza è un processo continuo. Iscriviti per ricevere insights, case studies e best practices direttamente nella tua inbox.
            </p>
          </motion.div>

          <motion.form
            onSubmit={handleFeedbackSubmit}
            variants={itemVariants}
            className="flex gap-3 mb-4"
          >
            <input
              type="email"
              placeholder="La tua email"
              value={email}
              onChange={(e) => setEmail(e.target.value)}
              className="flex-1 px-4 py-3 rounded-lg border border-border bg-background text-foreground placeholder:text-muted-foreground focus:outline-none focus:ring-2 focus:ring-accent"
              required
            />
            <Button
              type="submit"
              size="lg"
              className="bg-accent hover:bg-accent/90 text-white"
            >
              <Send className="w-4 h-4" />
            </Button>
          </motion.form>

          {feedbackSubmitted && (
            <motion.div
              initial={{ opacity: 0, y: -10 }}
              animate={{ opacity: 1, y: 0 }}
              className="text-center text-accent font-medium"
            >
              ✓ Grazie! Ti contatterò presto.
            </motion.div>
          )}
        </motion.div>
      </section>

      {/* Services Section */}
      <section id="servizi" className="py-24">
        <motion.div
          className="container"
          variants={containerVariants}
          initial="hidden"
          whileInView="visible"
          viewport={{ once: true }}
        >
          <motion.div variants={itemVariants} className="mb-16">
            <span className="text-accent font-semibold text-sm">SERVIZI</span>
            <h2
              className="heading-xl mt-2 mb-4"
              style={{ fontFamily: "Playfair Display" }}
            >
              Cosa Offro
            </h2>
            <p className="body-lg text-muted-foreground max-w-2xl">
              Tre pilastri di competenza per trasformare la tua organizzazione.
            </p>
          </motion.div>

          <div className="grid md:grid-cols-3 gap-8">
            {[
              {
                title: "Cyber-Process Auditing",
                description:
                  "Mappo i tuoi processi con precisione ingegneristica, identificando ogni punto di vulnerabilità umana e tecnica.",
                icon: "🔍",
              },
              {
                title: "Security Coaching",
                description:
                  "Trasformo le minacce tecniche in abitudini sicure. Dal phishing awareness alla password hygiene, per ogni membro del team.",
                icon: "👥",
              },
              {
                title: "Technical Communication",
                description:
                  "Rendo visibile l'invisibile. Documentazione, briefing e comunicazione che i tuoi team effettivamente leggono e capiscono.",
                icon: "📝",
              },
            ].map((service, idx) => (
              <motion.div
                key={idx}
                variants={itemVariants}
                whileHover={{ y: -8 }}
              >
                <Card className="p-8 border-border/50 hover:border-accent/50 transition-smooth h-full">
                  <div className="text-4xl mb-4">{service.icon}</div>
                  <h3 className="heading-md mb-3">{service.title}</h3>
                  <p className="text-muted-foreground">{service.description}</p>
                </Card>
              </motion.div>
            ))}
          </div>
        </motion.div>
      </section>

      {/* Continuous Improvement Section */}
      <section className="py-24 bg-card/30">
        <motion.div
          className="container"
          variants={containerVariants}
          initial="hidden"
          whileInView="visible"
          viewport={{ once: true }}
        >
          <motion.div variants={itemVariants} className="mb-16">
            <span className="text-accent font-semibold text-sm">FEEDBACK LOOP</span>
            <h2
              className="heading-xl mt-2 mb-4"
              style={{ fontFamily: "Playfair Display" }}
            >
              Mai Statico, Sempre in Evoluzione
            </h2>
            <p className="body-lg text-muted-foreground max-w-2xl">
              Il feedback continuo è il cuore della sicurezza moderna. Misuro, imparo e miglioro costantemente per mantenerti sempre un passo avanti.
            </p>
          </motion.div>

          <motion.div
            className="rounded-2xl overflow-hidden"
            variants={itemVariants}
            whileInView={{ opacity: 1, y: 0 }}
            initial={{ opacity: 0, y: 20 }}
            viewport={{ once: true }}
          >
            <img
              src="https://d2xsxph8kpxj0f.cloudfront.net/310519663629168585/2PoPXpkUQD2fwnKo7Cydp8/feedback-loop-visual-hAD8yLCqrXVRZtXEyBTmY5.webp"
              alt="Feedback Loop"
              className="w-full h-auto"
            />
          </motion.div>
        </motion.div>
      </section>

      {/* Case Studies Section */}
      <section className="py-24">
        <motion.div
          className="container"
          variants={containerVariants}
          initial="hidden"
          whileInView="visible"
          viewport={{ once: true }}
        >
          <motion.div variants={itemVariants} className="mb-16">
            <span className="text-accent font-semibold text-sm">CASE STUDIES</span>
            <h2
              className="heading-xl mt-2 mb-4"
              style={{ fontFamily: "Playfair Display" }}
            >
              Progetti Reali, Risultati Concreti
            </h2>
            <p className="body-lg text-muted-foreground max-w-2xl">
              Scopri come ho trasformato la sicurezza di organizzazioni come la tua. Ogni progetto è una storia di consapevolezza, implementazione e miglioramento continuo.
            </p>
          </motion.div>

          <CaseStudies />
        </motion.div>
      </section>

      {/* Contact Form Section */}
      <section id="contatti" className="py-24 bg-card/30">
        <motion.div
          className="container"
          variants={containerVariants}
          initial="hidden"
          whileInView="visible"
          viewport={{ once: true }}
        >
          <motion.div variants={itemVariants} className="text-center mb-12">
            <span className="text-accent font-semibold text-sm">CONTATTI</span>
            <h2
              className="heading-xl mt-2 mb-4"
              style={{ fontFamily: "Playfair Display" }}
            >
              Iniziamo il Viaggio Insieme
            </h2>
            <p className="body-lg text-muted-foreground max-w-2xl mx-auto">
              Hai una sfida di sicurezza? Compila il form qui sotto e ti contatterò entro 24 ore per discutere come posso aiutarti.
            </p>
          </motion.div>

          <ContactForm />
        </motion.div>
      </section>

      {/* CTA Section */}
      <section className="py-24 bg-gradient-to-r from-foreground to-foreground/90 text-background">
        <motion.div
          className="container text-center"
          variants={containerVariants}
          initial="hidden"
          whileInView="visible"
          viewport={{ once: true }}
        >
          <motion.h2
            variants={itemVariants}
            className="display-md mb-6"
            style={{ fontFamily: "Playfair Display" }}
          >
            Costruiamo una Cultura Resiliente
          </motion.h2>
          <motion.p
            variants={itemVariants}
            className="body-lg mb-8 max-w-2xl mx-auto opacity-90"
          >
            La sicurezza è un processo, non un prodotto. Che tu abbia bisogno di un audit, coaching del team o documentazione tecnica, iniziamo il viaggio insieme.
          </motion.p>
          <motion.div variants={itemVariants} className="flex gap-4 justify-center">
            <Button
              size="lg"
              className="bg-background text-foreground hover:bg-background/90"
            >
              Contattami su LinkedIn
            </Button>
          </motion.div>
        </motion.div>
      </section>

      {/* Footer */}
      <footer className="border-t border-border py-12">
        <motion.div
          className="container"
          initial={{ opacity: 0 }}
          whileInView={{ opacity: 1 }}
          viewport={{ once: true }}
        >
          <div className="flex flex-col md:flex-row justify-between items-center gap-8">
            <div>
              <p className="font-semibold mb-2">Vale Segatti</p>
              <p className="text-sm text-muted-foreground">
                Human-Centric Security Engineering
              </p>
            </div>
            <div className="flex gap-6 text-sm text-muted-foreground">
              <a href="#approccio" className="hover:text-accent transition-smooth">
                Approccio
              </a>
              <a href="#servizi" className="hover:text-accent transition-smooth">
                Servizi
              </a>
              <a href="#contatti" className="hover:text-accent transition-smooth">
                Contatti
              </a>
            </div>
          </div>
          <div className="mt-8 pt-8 border-t border-border/50 text-center text-sm text-muted-foreground">
            <p>© 2026 Vale Segatti. Tutti i diritti riservati.</p>
          </div>
        </motion.div>
      </footer>
    </div>
  );
}

